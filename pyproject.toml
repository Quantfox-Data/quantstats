[tool.poetry]
name = "quantstats"
version = "0.1.0"
description = "stat tools for returns"
authors = ["FIEVET Cyp <cyprienfievet@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
